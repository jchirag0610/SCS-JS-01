// Single line comment

/* If you would have multiple lines of text or code that you don't want to run or ignore by interpreter(something that executes the statement line by line unlike the compiler that executes all statement at once) then
 need to use multiline Comment*/
